import { Metadata } from "next";

export const metadata: Metadata = {
  title: "pricing",
  description: "pagina de pricing",
};

const PricingPage = () => {
  return <div>desde pricing page</div>;
};

export default PricingPage;
