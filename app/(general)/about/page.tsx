import { Metadata } from "next";

export const metadata: Metadata = {
  title: "about",
  description: "pagina de about",
};

const AboutPage = () => {
  return <div>hola desde about</div>;
};

export default AboutPage;
