import { Metadata } from "next";

export const metadata: Metadata = {
  title: "contact",
  description: "pagina de contact",
};

const ContactPage = () => {
  return <div>desde contact page</div>;
};

export default ContactPage;
